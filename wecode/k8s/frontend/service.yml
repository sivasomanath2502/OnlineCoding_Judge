apiVersion: v1
kind: Service
metadata:
  name: wecode-frontend
  namespace: wecode
spec:
  selector:
    app: wecode-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort